# Open Questions

Unresolved questions surfaced during planning. One dated bullet per question, with the plan slug that raised it. Append-only.

- 2026-06-26 — [batch-creative-studio-mvp] **Provider credentials provisioning.** ~~`GEMINI_API_KEY` and Cloudflare (`CLOUDFLARE_ACCOUNT_ID` + `CLOUDFLARE_API_TOKEN`) must be provisioned before tasks 3/13.~~ **RESOLVED 2026-06-26:** env scaffolding created (`.env.example` tracked template + gitignored `.env.local`); secret-management strategy recorded in `decisions.md`. Remaining action is purely a developer setup step — paste the obtained keys into `.env.local` (Gemini: aistudio.google.com/apikey · Cloudflare: dash.cloudflare.com → Workers AI + API Tokens). `BLOB_READ_WRITE_TOKEN` arrives at Vercel Blob connect (T3/T13). No longer an open design question.
- 2026-06-26 — [batch-creative-studio-mvp] **Provider/platform API shapes unverified at plan time.** Gemini 2.5 Flash Image edit, Cloudflare Workers AI image, Vercel Blob signed client upload, and Next.js SSE `ReadableStream` to be Context7-verified during execution (T3/T4/T6/T8). Flagged, not yet resolved.
- 2026-06-26 — [batch-creative-studio-mvp] **Upload contract diverges from architecture §7.2 — frontend follow-up required (raised by T3, backend).** Context7 verification of `@vercel/blob` (v2.5.0) confirmed the real client-upload mechanism is a two-phase **token exchange** via the server `handleUpload()` helper + the client `upload()` from `@vercel/blob/client` — **there is no presigned-`PUT`-URL flow** returning `{ uploadUrl, blobUrl }` as architecture §7.2 / product-flow §7 idealize. The backend route `app/api/uploads/route.ts` (T3) is implemented against the **real** API. **Cross-task action for Task 2 frontend (`lib/client/uploadClient.ts`, NOT edited by backend):** replace the manual `requestSignedUpload` + raw `PUT` with `import { upload } from "@vercel/blob/client"` and call `upload(\`uploads/${kind}/${file.name}\`, file, { access: "public", handleUploadUrl: "/api/uploads", contentType: file.type, clientPayload: JSON.stringify({ kind }) })`, returning `result.url` as the durable blobUrl. The route enforces: `pathname` must start with `uploads/${kind}/`; `clientPayload` must be `JSON.stringify({ kind })` with `kind ∈ {product, reference}`; content-type allowlist + 10 MB cap are baked into the minted token (authoritative at Blob). The frontend also needs `@vercel/blob` available client-side (already a project dep after T3). The `SignedUploadResponse {uploadUrl, blobUrl}` type and the `"sign"|"put"` `UploadError` steps in `uploadClient.ts` no longer match the wire contract and should be reworked to the `upload()` flow. Unresolved until the frontend agent actions it.
